<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:id="@+id/loading"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:alpha="0"
    android:background="@android:color/white"
    android:gravity="center"
    android:orientation="vertical">

    <android.support.v4.widget.ContentLoadingProgressBar
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="@dimen/game_detail_loading_size"
        android:layout_height="@dimen/game_detail_loading_size"
        android:layout_gravity="center"/>

    <TextView
        style="@style/AppTheme.Loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="@string/gamerelation_detail_loading"/>

</LinearLayout>