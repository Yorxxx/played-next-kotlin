package com.piticlistudio.playednext.domain.mapper.game

internal class GameMapperTest {

    /*@Nested
    @DisplayName("Given a GameMapper instance")
    inner class GameMapperInstance {

        private lateinit var mapper: GameMapper

        @BeforeEach
        internal fun setUp() {
            mapper = GameMapper()
        }

        @Nested
        @DisplayName("When mapFromModel is called")
        inner class mapFromModel {

            private val entity = GameEntity(1, "name", "summary", "storyline",
                    10, 20, 95f, null)
            private var result: Game? = null

            @BeforeEach
            internal fun setUp() {
                result = mapper.mapFromModel(entity)
            }

            @Test
            @DisplayName("Then should map into Game entity")
            fun shouldMap() {
                assertNotNull(result)
                result?.apply {
                    assertEquals(entity.id, id)
                    assertEquals(entity.name, name)
                    assertEquals(entity.summary, summary)
                    assertEquals(entity.storyline, storyline)
                }
            }
        }
    }*/
}